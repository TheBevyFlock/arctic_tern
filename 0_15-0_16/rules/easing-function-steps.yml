# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json

id: easing-function-steps
language: rust
rule:
  pattern: EaseFunction::Steps($ARG)
fix: EaseFunction::Steps($ARG, JumpAt::default())
